# 项目改进点

> 基于简历和面试准备稿梳理的两个个人项目的可改进方向，供面试时应对追问或展示技术深度用。

---

## Mini-OpenClaw

### 1. 沙箱隔离不够彻底

**现状**：terminal 工具用正则黑名单拦截破坏性命令，read_file 用路径限制防穿越，但 python_repl 在同一进程内执行，`shutil.rmtree("/")` 等纯 Python 调用拦不住。

**改进方向**：
- python_repl 改为 subprocess 隔离执行，设置 resource limit（rlimit 限制 CPU/内存）、chroot 或 namespace 隔离文件系统
- 参考容器 runtime 的思路，用 cgroup 限制子进程资源，用 seccomp 过滤系统调用
- 终端工具也可以改为 subprocess 执行，配合白名单机制（只允许安全命令）替代黑名单

### 2. 图谱构建比较粗糙

**现状**：关键词提取用正则+词频，只取英文词 top 12，没有语义理解能力；边类型只有 5 种，关联关系主要靠文件引用和词频共现。

**改进方向**：
- 用 LLM 做实体识别和关系抽取，替代正则+词频的方案，提升节点和边的语义质量
- 引入 embedding 相似度作为边权重，让检索时能按语义相关性排序
- 增加边类型：比如"同一文档内的概念共现"、"trace 中的错误模式关联"等

### 3. 检索评测体系可以更完善

**现状**：评测框架支持 ablation profile，但检索效果只有人工判断，没有自动化评测指标。

**改进方向**：
- 引入 IR 领域标准指标：Recall@K、MRR、Hit@1、nDCG
- 构建标注数据集：人工标注 query-document 相关性对
- 和 Knowledge Agent 项目一样做跨检索模式对比（BM25 vs 向量 vs 混合 vs 图谱增强）

### 4. 流式输出的中间状态可以更细

**现状**：已经用 FastAPI + SSE（sse-starlette）实现了流式推送，中间推理过程和工具调用结果实时推到前端。但当前推送粒度是"工具调用完成"和"最终回答"，缺少 token 级别的流式输出（LLM 生成过程中逐 token 推送）。

**改进方向**：
- 接入 LLM 的 token stream，实现逐 token 推送，让用户看到模型"打字"的过程
- 当前工具调用结果已经能实时推送，但推理阶段的中间思考过程还可以更细

### 5. 状态管理比较简单

**现状**：Agent 每次请求独立，没有跨会话的状态持久化。

**改进方向**：
- 加 memory 模块，用向量数据库存储历史对话 embedding，下次对话时检索相关历史
- 当前 ablation 评测中 no_memory profile 已经预留了这个能力的测试位

---

## Knowledge Agent

### 1. 向量检索用 SQLite + 余弦相似度，性能有上限

**现状**：用 SQLite 存向量，检索时全表扫描算余弦相似度，数据量大了会很慢。

**改进方向**：
- 引入 HNSW 索引（如 hnswlib 或 qdrant）做近似最近邻搜索，把检索从 O(n) 降到 O(log n)
- 或者用 FAISS 做内存级向量索引，适合单机部署场景
- 当前方案的优势是不依赖外部服务，部署简单，适合个人知识管理的规模

### 2. subagent 扫描策略可以更智能

**现状**：深层问题触发 subagent 扫描时，对仓库文件按 topic 打分筛选 top-8，提取代码片段后由 LLM 综合分析。

**改进方向**：
- 加入代码结构感知：用 AST 解析识别函数/类定义，优先提取与问题相关的代码结构而非全文
- 多轮检索：第一轮粗筛后，根据 LLM 反馈决定是否需要第二轮更细粒度的检索
- 缓存策略可以加入 fingerprint 变化检测，只在文件变更时重新分析

### 3. DAG 工作流的错误恢复粒度较粗

**现状**：节点通过 subprocess 隔离执行，支持独立超时与失败回滚，但回滚是整个 DAG 级别的。

**改进方向**：
- 实现节点级别的重试：单个节点失败后只重试该节点，不需要重新执行整个 DAG
- 加入断点续执行：记录 DAG 执行进度，中断后从失败节点恢复
- 参考 LangGraph 的 checkpoint 机制，每个节点执行后保存状态快照

### 4. 检索模式切换是手动的

**现状**：BM25/向量/混合三种检索模式支持运行时动态切换，但需要用户主动选择。

**改进方向**：
- 自动路由：根据 query 特征（关键词密度、问题类型、是否包含专有名词）自动选择最佳检索模式
- 或者用 LLM 做 query 分类，决定用哪种检索策略
- 记录每种模式在不同 query 类型下的表现，用历史数据优化路由策略

### 5. 缺少多用户隔离

**现状**：单用户设计，所有项目共享同一个数据库。

**改进方向**：
- 加用户认证和项目级隔离
- 每个项目的向量库和知识库独立
- 支持项目间共享知识（可选）

---

## 两个项目的共性改进

### 1. 测试覆盖不足

两个项目都缺少单元测试和集成测试。改进方向：
- Mini-OpenClaw：为 RAG 检索、图谱构建、沙箱拦截写单元测试
- Knowledge Agent：为 DAG 节点执行、向量检索、缓存逻辑写单元测试
- 两个项目都可以用 pytest + coverage 做持续集成

### 2. 缺少可观测性

两个项目都没有结构化日志和指标采集。改进方向：
- 加 structured logging（JSON 格式），方便后续分析
- 关键路径加耗时打点（检索延迟、LLM 调用延迟、DAG 节点执行时间）
- 考虑接入 OpenTelemetry 做分布式追踪

### 3. 配置管理可以更规范

两个项目的配置（模型参数、检索参数、路径等）散落在代码和环境变量中。改进方向：
- 统一用 pydantic Settings 做配置管理
- 支持 .env 文件和环境变量覆盖
- 配置项加类型检查和默认值
