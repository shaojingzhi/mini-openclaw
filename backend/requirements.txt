# Mini-OpenClaw backend Python dependencies.
# All versions are pinned for reproducibility.

# --- Web framework ---
fastapi==0.135.1
uvicorn[standard]==0.41.0
sse-starlette==3.4.4

# --- Validation / config ---
pydantic==2.12.5
python-dotenv==1.2.2

# --- LangChain 1.x stable (Agent runtime) ---
# create_agent lives in langchain.agents; langchain-community / -experimental
# carry the built-in tools we wrap (ShellTool, ReadFileTool, RequestsGetTool,
# PythonREPLTool).
langchain==1.3.4
langchain-community==0.4.2
langchain-experimental==0.4.2
langchain-openai==1.2.2

# --- LlamaIndex (RAG / hybrid retrieval) ---
llama-index-core==0.14.22
llama-index-retrievers-bm25==0.7.1

# --- HTML cleaning for fetch_url tool ---
beautifulsoup4==4.14.3
html2text==2025.4.15
